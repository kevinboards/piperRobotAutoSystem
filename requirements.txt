# Piper Automation System - Requirements
# Python 3.8+ required

# Core dependency - Piper Robot SDK
piper_sdk>=0.6.1

# CAN bus communication (required by piper_sdk)
python-can>=4.3.1

# Type checking support
typing-extensions>=4.5.0

# Note: tkinter is included with Python standard library
# If tkinter is missing, install via system package manager:
#   Ubuntu/Debian: sudo apt-get install python3-tk
#   Fedora/RHEL: sudo dnf install python3-tkinter
#   Windows/Mac: Included with Python installer

